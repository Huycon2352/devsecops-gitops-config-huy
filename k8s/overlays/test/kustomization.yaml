apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Trỏ trực tiếp đến thư mục chứa Helm Chart gốc
resources:
  - ../../chart

# Ép Service của UI thành NodePort cho môi trường test
patches:
  - path: patch-ui-service.yaml