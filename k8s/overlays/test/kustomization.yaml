apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bắt Kustomize lấy trực tiếp tài nguyên từ thư mục chart gốc làm nền tảng
resources:
  - ../../chart

# Áp file patch đổi NodePort vào
patches:
  - path: patch-ui-service.yaml