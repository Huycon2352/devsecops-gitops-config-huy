apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Nạp file manifest tổng hợp đã được sinh từ Helm ở Bước 1
resources:
  - all-manifests.yaml

# Áp file patch để chuyển Service UI thành NodePort cho Minikube/Kind
patches:
  - path: patch-ui-service.yaml